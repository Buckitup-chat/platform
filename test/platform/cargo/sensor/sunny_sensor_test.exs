defmodule Platform.Cargo.Sensor.SunnySensorTest do
  @moduledoc """
  Sunny scenario of cargo

  - cargo_user created
  - camera sensors and weight sensors configured
  - flash drive with cargo room and invite in cargo_user
  - flash drive mounted and scenario goes well
  """
  use ExUnit.Case, async: true

  test "sensor messages should be written into cargo_room"
  test "indication is correct"
end
